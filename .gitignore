# Python
__pycache__/
*.pyc

# Environnement virtuel
.venv/

# Base de données binaire (générée, non versionnée)
knowledge.duckdb
knowledge.duckdb.wal
knowledge.backup-*.duckdb
# Suivi de progression de la réindexation vision (local, régénérable)
knowledge.reindex-vision-done.json

# Documents sources (l'utilisateur fournit le chemin à l'ingestion, aucune
# convention de dossier imposée — ne pas committer les documents métier).
# Ajoutez ici vos chemins si besoin, ex :
# /data/
# /docs/

# Config agent locale (spécifique à l'environnement de dev)
.agents/
.zcode/

# Divers
.DS_Store

# Résultats de benchmark (régénérables, volumineux) — les scripts restent versionnés
bench/results*.jsonl
bench/results*.md
bench/REPORT_chunks_final.md
